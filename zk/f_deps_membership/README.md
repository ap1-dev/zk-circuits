# F_deps_membership

This circuit proves that the dependencies used inside the TEE are a subset of the approved dependency list.

## Public inputs

```text
approved_deps_commitment
used_deps_commitment